🚀 Introdução à Programação Orientada a Objetos (POO) em Java

Este repositório contém uma série de exercícios práticos em Java focados na criação e manipulação de classes

✨ Conceitos Abordados

Cada classe foi projetada para representar uma entidade do mundo real, implementando:

Encapsulamento: Uso de atributos `private` e métodos `public` (Getters e Setters) para controlar o acesso aos dados.
Construtores: Métodos especiais para inicialização de objetos. 
Métodos de Negócio: Lógica específica da entidade, como `calcularMedia()`, `depositar()`, `aumentarSalario()`, e `aplicarDesconto()`.
Herança: A classe `Funcionario` estende a classe `Pessoa`, demonstrando reutilização de código.

⚙️ Como Executar o Projeto

Para testar todas as funcionalidades das classes, siga os passos abaixo no seu ambiente Java/Terminal:

1.  Clone o Repositório:

    ```bash
    git clone [https://github.com/diegomoraespro/exercicio-classe-2025-2.git](https://github.com/diegomoraespro/exercicio-classe-2025-2.git)
    cd exercicio-classe-2025-2
    ```

2.  Compile a Classes:

    ```bash
    javac Main.java
    ```
3.  Execute a Classe Principal:

    ```bash
    java Main
    ```
    
    O console exibirá as operações de criação, modificação de dados e chamadas aos métodos de negócio de cada classe.
